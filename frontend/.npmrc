# frontend/.npmrc

# Use legacy peer deps to handle react-scripts version conflicts
legacy-peer-deps=true

# Increase timeout for slow connections
fetch-timeout=300000
fetch-retries=3

# Use prefer-offline instead of deprecated cache-min
prefer-offline=true